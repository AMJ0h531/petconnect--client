npm install twilio
const client = require("twilio")(
  process.env.TWILIO_SID,
  process.env.TWILIO_AUTH
);

client.messages.create({
  body: "Your pet adoption is approved!",
  from: process.env.TWILIO_NUMBER,
  to: user.phone
});
